/**
 * Education document from Sanity CMS.
 * Schema: `education`
 */
export interface Education {
  level: string;
  degree: string;
  institution: string;
  startDate: string;
  endDate?: string;
}
